<!--
  Fallback for the homepage "Why" section, used only when the admin panel's
  "Custom section" markdown field is empty for this tenant.

  Empty by default, so nothing renders. Add your own HTML below for a section
  that goes beyond what the markdown field can express (custom layout, embeds,
  multiple columns, etc.) — it renders as-is, with no wrapping added for you.
-->
