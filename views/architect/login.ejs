<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Architect Admin Login</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; }
    body { font-family: system-ui, sans-serif; background: #f4f4f5; display: flex; align-items: center; justify-content: center; min-height: 100vh; margin: 0; }
    .card { background: #fff; border: 1px solid #e4e4e7; border-radius: 6px; padding: 2rem; width: 100%; max-width: 360px; }
    h1 { font-size: 1.2rem; margin: 0 0 1.5rem; }
    label { display: block; font-size: .85rem; font-weight: 600; margin-bottom: .3rem; }
    input[type=password] { display: block; width: 100%; padding: .5rem .6rem; border: 1px solid #d4d4d8; border-radius: 4px; font-size: 1rem; margin-bottom: 1rem; }
    input[type=password]:focus { outline: 2px solid #333; outline-offset: 1px; }
    button { width: 100%; padding: .55rem; background: #18181b; color: #fff; border: none; border-radius: 4px; font-size: .95rem; cursor: pointer; }
    button:hover { background: #3f3f46; }
    .error { background: #fef2f2; color: #b91c1c; border: 1px solid #fecaca; border-radius: 4px; padding: .5rem .75rem; font-size: .85rem; margin-bottom: 1rem; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Architect Admin Login</h1>
    <% if (locals.error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form method="POST" action="/architect/login">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <label for="password">Password</label>
      <input type="password" id="password" name="password" autofocus autocomplete="current-password">
      <button type="submit">Log in</button>
    </form>
  </div>
</body>
</html>
